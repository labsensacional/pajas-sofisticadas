{
  "name": "pajas-sofisticadas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev --port 5174",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch"
  },
  "dependencies": {
    "firebase": "^10.12.4"
  },
  "devDependencies": {
    "@sveltejs/adapter-vercel": "^5.10.3",
    "@sveltejs/kit": "^2.53.4",
    "svelte": "^5.0.0",
    "svelte-check": "^3.8.0",
    "typescript": "^5.4.0",
    "vite": "^6.3.0"
  }
}
