# Pajas Sofisticadas

MVP web (SvelteKit + Firebase).

## Dev

```bash
nvm use
npm install
npm run dev
```

## Deploy

- Frontend: Vercel
- Firebase: `firebase deploy --only firestore:rules,storage`

## Env

Completa `.env` con las variables `VITE_FIREBASE_*`.
